# Templates

Store reusable document structures here.
Examples: email templates, proposal outlines, weekly report format.
Claude will use these as the starting point for recurring deliverables.
Never overwrite the originals — Claude will adapt them per use.
